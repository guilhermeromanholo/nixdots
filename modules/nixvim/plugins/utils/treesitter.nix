{
  plugins.treesitter = {
    enable = true;

    # folding = true;

    settings = {
      auto_install = true;
      indent.enable = true;
      highlight.enable = true;
    };
  };

  plugins.treesitter-context = {
    enable = true;
  };
}
